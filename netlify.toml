[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "16"

[build]
  publish = "dist"
  command = "yarn install && yarn generate"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
